ANTHROPIC_API_KEY=sk-ant-your-key-here
# Comma-separated list of allowed CORS origins. Defaults to * (all) if not set.
# Example: https://your-app.vercel.app,https://www.your-domain.com
ALLOWED_ORIGINS=*
# API-Football key (api-sports.io) — required for match mode lineup fetching
# Free tier: 100 requests/day — register at https://www.api-football.com
API_FOOTBALL_KEY=your-api-football-key-here
